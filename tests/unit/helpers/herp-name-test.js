import { herpName } from '../../../helpers/herp-name';
import { module, test } from 'qunit';

module('Unit | Helper | herp name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = herpName(42);
  assert.ok(result);
});
